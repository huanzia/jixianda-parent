<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.jixianda.mapper.WarehouseMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into warehouse(name, location, address, contact_name, phone, status)
        values (#{name}, #{location}, #{address}, #{contactName}, #{phone}, #{status})
    </insert>

    <update id="update">
        update warehouse
        <set>
            <if test="name != null">name = #{name},</if>
            <if test="location != null">location = #{location},</if>
            <if test="address != null">address = #{address},</if>
            <if test="contactName != null">contact_name = #{contactName},</if>
            <if test="phone != null">phone = #{phone},</if>
            <if test="status != null">status = #{status},</if>
        </set>
        where id = #{id}
    </update>

    <select id="getById" resultType="com.jixianda.entity.Warehouse">
        select id, name, location, address, contact_name, phone, status
        from warehouse
        where id = #{id}
    </select>

    <delete id="deleteBatch">
        delete from warehouse
        where id in
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

    <select id="pageQuery" resultType="com.jixianda.entity.Warehouse">
        select id, name, location, address, contact_name, phone, status
        from warehouse
        <where>
            <if test="name != null and name != ''">
                and name like concat('%', #{name}, '%')
            </if>
        </where>
        order by id desc
        limit #{offset}, #{pageSize}
    </select>

    <select id="count" resultType="long">
        select count(1)
        from warehouse
        <where>
            <if test="name != null and name != ''">
                and name like concat('%', #{name}, '%')
            </if>
        </where>
    </select>

    <update id="updateStatus">
        update warehouse
        set status = #{status}
        where id = #{id}
    </update>

    <select id="list" resultType="com.jixianda.entity.Warehouse">
        select id, name, location, address, contact_name, phone, status
        from warehouse
        <where>
            <if test="status != null">
                and status = #{status}
            </if>
        </where>
        order by id asc
    </select>

</mapper>
